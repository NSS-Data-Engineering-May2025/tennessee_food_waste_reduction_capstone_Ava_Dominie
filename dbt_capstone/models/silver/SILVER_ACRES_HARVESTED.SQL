{{ config(materialized='table') }}

SELECT
	YEAR,
	COUNTY_NAME,
	COUNTY_CODE,
	STATE_NAME,
	STATE_ALPHA,
	STATE_FIPS_CODE,
	COMMODITY_DESC,
	VALUE AS ACRES_HARVESTED,
	UNIT_DESC,
	STATISTICCAT_DESC
FROM {{ source('BRONZE', 'ACRES_HARVESTED') }}
WHERE VALUE IS NOT NULL