{{ config(materialized='table') }}

SELECT
	YEAR,
	COUNTY_NAME,
	COUNTY_CODE,
	STATE_NAME,
	STATE_ALPHA,
	STATE_FIPS_CODE,
	COMMODITY_DESC,
	VALUE AS PRODUCTION,
	UNIT_DESC,
	STATISTICCAT_DESC
FROM {{ source('BRONZE', 'PRODUCTION') }}
WHERE VALUE IS NOT NULL
